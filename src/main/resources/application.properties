spring.application.name=demo
# Don't change the port
server.port = 9001
# for https
server.forward-headers-strategy=framework

spring.datasource.url=jdbc:mysql://localhost:3306/transport?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT secret key (keep it strong and long for production)
jwt.secret=MySuperSecretKey123456
# Optional: token expiration time in milliseconds
jwt.expiration=3600000
# Server port (you already set 9001)
server.port=9001

server.port=9001
jwt.secret=MySuperSecretKey12345
jwt.expiration=3600000   # optional, for token expiration
